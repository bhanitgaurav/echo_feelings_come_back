ktor {
    deployment {
        port = 8081
        port = ${?PORT}
    }
    application {
        modules = [ com.bhanit.apps.echo.ApplicationKt.module ]
    }
}

features {
    emotionLimits {
        dailyCap = 50 // Test limits
    }
    cooldowns {
        enabled = true
        durationMinutes = 2 // Short cooldown for testing
    }
    safety {
        shadowBanEnabled = true
    }
}

environment = "qa"
